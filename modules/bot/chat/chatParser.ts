import IncomingMessage from "./incomingMessage";

export default class ChatParser {

    public static parse(message: IncomingMessage) {

        //There is a lot to parse here

        //Chat messages

        console.log(message);
    }
}